class Config:
    def __init__(self, run, details, random, params, auxiliary):
        self.run = run
        self.details = details
        self.random = random
        self.params = params
        self.auxiliary = auxiliary
